
type Props = { onResetSuccess: () => void };

const ResetPasswordPage = ({onResetSuccess}: Props)=>{
    return <p className="text-wrap w-80 mt-2"> {"It is unfortunate but this feature is not available right now. It will probably be done soon. Perhaps you could try to remember your password in the meantime :)"}</p>
}

export default ResetPasswordPage;